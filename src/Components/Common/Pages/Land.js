import React from 'react';
import { useState, useEffect } from 'react';
import { Select, SelectItem, Input, Button, Card, CardBody, DatePicker, DateRangePicker, Chip } from '@nextui-org/react';
import { CalendarDate, parseDate } from "@internationalized/date";
const Land = () => {

    return (
        <div 
            className={`relative min-h-screen flex`}
            style={{backgroundColor: '#F4D30F'}}
        >
            <h1 className="container mx-auto flex justify-center items-center text-4xl ">
                Mario Di Marino
            </h1>
            <p> prova </p>
        </div>
    );
};


export default Land;

